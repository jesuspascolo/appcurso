import { EuroformaterPipe } from './euroformater.pipe';

describe('EuroformaterPipe', () => {
  it('create an instance', () => {
    const pipe = new EuroformaterPipe();
    expect(pipe).toBeTruthy();
  });
});
